{
    "framework": "nextjs",
    "buildCommand": "pnpm turbo build",
    "installCommand": "pnpm install",
    "outputDirectory": "snake-game-client/.next",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/$1"
      }
    ]
  }