{
    "framework": "nextjs",
    "buildCommand": "pnpm turbo build",
    "installCommand": "pnpm install",
    "outputDirectory": ".next",
    "rewrites": [
      {
        "source": "/(.*)",
        "destination": "/$1"
      }
    ]
  }