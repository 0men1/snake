{
    "framework": "nextjs",
    "buildCommand": "cd ../shared && pnpm install && pnpm build && cd ../snake-game-client && pnpm install && pnpm build",
    "installCommand": "pnpm install",
    "outputDirectory": ".next",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/$1"
        }
    ]
}